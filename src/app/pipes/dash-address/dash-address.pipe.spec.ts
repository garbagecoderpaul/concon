import { DashAddressPipe } from './dash-address.pipe';

describe('DashAddressPipe', () => {
  it('create an instance', () => {
    const pipe = new DashAddressPipe();
    expect(pipe).toBeTruthy();
  });
});
