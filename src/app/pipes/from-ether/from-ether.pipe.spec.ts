import { FromEtherPipe } from './from-ether.pipe';

describe('FromEtherPipe', () => {
  it('create an instance', () => {
    const pipe = new FromEtherPipe();
    expect(pipe).toBeTruthy();
  });
});
